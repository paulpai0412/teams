# Task Pi Runtime TODO

更新：2026-09-14；設計 v1.19。主代理實作，不啟基礎建設 child。

**最新：依授權實作通用 Task 共用預算池，非 G1 配額特例。** 新 v3 預設 shared，角色估算不再作終身硬 cap；沿既有 ledger 原子預留／結算，Worker、leaf、source-bound review 與 final acceptance 共用語義。Task ceiling、歷史成本、未知用量、owner／deadline／cancel、source／receipt gates 保留；舊契約不變。14 個 profiles 只加 budget hook，模型與工具權限不改。最終相關回歸 **368/368**、65 檔 source/profile 前後 SHA 相同；budget-only 入口另驗普通 in-process roles 不註冊 Worker tools/hooks。既有 model-settings 另跑仍 3/4（舊 tools 斷言），不改權限求全綠。記錄見 [shared-budget evidence](goal-team-evidence/task-runtime-shared-budget-20260914/README.md)。本輪未 live／G1／新 Goal／reload／正式 ledger migration；不能用離線測試關閉 request-to-outcome、舊 r2 recovery、r1 Goal 或 campaign anchor／完整計量缺口。以下歷史 checkpoint 保留原語義。

**最新：依使用者授權修復 detached runner 與 no-start 交接（離線已驗，不是 live PASS）。** 本機 pi-subagents 0.66.0 移除無 consumer 的兩項 chord 必要 alias；runner 各準備步驟在確定尚未 spawn 且 durable proof 保存成功時，沿既有 ProcessTerminal/1＋RPC details 交給 Task。Worker role／L0 review 共用身分核對與既有 capture；failed 不變 successful，不重派，取消／歷史計量可處理無 child session 的 run，先前 Worker／leaf 成本保留。spawn 後不明、revival、證據缺失／不符仍 unknown，不刪 assertion、不新增 controller／ledger。最終相關 **342/342**；另有既有 model-settings tools 斷言不符，在修前副本也重現，未順便改動。完整記錄／patch：[launch failure 修復](goal-team-evidence/task-runtime-launch-failure-fix-20260914/README.md)。本輪未模型／Goal／browser／reload／live重跑／舊run recovery。r2 execution `03c35db0-e147-49f1-8e50-71c52ca6a161` 唯讀仍 CANCEL_REQUESTED/reservation open；原 Worker PID 已查無，退出原因未證，不稱 cleanup。r1 Goal、歷史 inventory、fresh native entrypoint 與 request-to-outcome 仍未結案。

**現行方向：通用使用者需求 → L0 產生 Task specs → Teams 執行／必要整合 → 原始需求驗收；不以通過 G1 為目標。** 已將 [L0 SPEC](extensions/teams-orchestrator/SPEC.md) 接到現有dispatch工具的description／原生promptGuidelines；L0用既有read/write寫spec，無新generator／tool／controller／schema。Worker prompt修正`scope.sourcePaths`錯讀，傳遞真正workspace範圍、allowed writes與contextRefs；不再靠把patch/path硬塞測試objective補交接。G1專用App＋guide產生器及checker撤至 `goal-team-evidence/task-runtime-request-flow-20260914/withdrawn-g1/`，舊有效receipts／失敗不改。共用E2E預算修正保留，僅是protocol runner能力，不冒稱全域總帳。Matrix v2.2改request-driven為必驗主線、G1/G2/G3為可重用protocol子案例。使用者另已核准常態載入，global settings只新增既有orchestrator extension路徑、其他設定不變；未reload／新模型／Goal／live。語意規劃與完整通用交付仍待真需求驗證，不能以離線測試關閉。證據 `goal-team-evidence/task-runtime-request-flow-20260914/README.md`。

以下checkpoint保留當時狀態；其中『依序重跑G1』及固定App＋guide準備規劃已由以上方向取代。

**最新：G1 測試準備／預算入口已離線修正，尚未重跑。** 首次 G1 已有真雙 Worker overlap、各 Receipt/3、雙 Task atomic batch/readback 與 cleanup，但 parent 誤用單 policy CLI（只預留5M、漏 campaign history）且兩案重複同一 App；L0 歸檔讀錯路徑後不是立即停止，而在 Goal completion 後由 observer 報 tool-failure。核心 Task runtime 無已證故障。本輪只改 E2E 入口：完整specPaths加總預留、固定budget parent anchor／ceiling、hash＋session identity歷史實耗，並計入啟動／執行／final budget checks；新G1是App＋獨立指南patch，parent負責歸檔，無runtime guard/retry/模型/gate変更。用法 `task-runtime/e2e/E2E-INPUTS.md`；證據 `goal-team-evidence/task-runtime-g1-preparation-fix-20260914/README.md`。下一次live前仍須完成真campaign歷史inventory核對並另獲重跑授權；不得使用空history或追認舊G1通過。

**E2E matrix v2.1（合併減量，2026-09-14）：** 依 `TASK-PI-RUNTIME-E2E-MATRIX-V2.md`，核心只規劃 **G1→G2→G3 三輪／共五個 Worker 啟動**，不再每個 case 各跑完整 E2E。G1 合併 C5 多 Task/batch、C5-W2 正例、C6 一次安全 input correction；G2 驗 C4 target apply／Receipt/2 並共用環境能力證據；G3 合併雙 Worker 取消隔離與 C7 cleanup，不再做 browser/review/產品交付。相較原核心各案分跑至少六輪／九 Workers，減少重複啟動；不保證 token 同比例下降。C8 若事前固定 matched 條件可引用 G2 作 Task Pi arm，只另跑 direct；C9 真升級時才重驗失效範圍，未驗不重標 PASS。既有有效離線證據引用不重跑；原 case IDs 保留 coverage，雙 Worker budget總預留／inventory／drain與 G2 公開互動確認須在 live 前核定。本次僅調整文件，無新 Goal／模型／runtime 變更。

**平行 Worker 核對更正（2026-09-14）：** 既有 L0 可依序 dispatch 不同 Task，兩個 Task Pi Worker 同時 RUNNING；單一 subagents workflow/session 限制不是全 L0 單 Worker 上限。公開 tool callbacks＋兩個真 Node WorkerRuntime process／v3 SQLite/mailbox 的回歸已驗握手、不同身份、重複 Task 拒絕、cancel A 不影響 B、Goal reservation guard 2→1→0；Herdr/Pi/模型為 fixtures，非 native outcome/live readiness。無需新增 parallel dispatch API，未修改 runtime。新增 **C5-W2 雙 Worker live E2E**，仍未執行；須核兩 Task 合計預留／實耗、managed mutation worktrees、兩邊 receipt/readback 與終態。證據 `goal-team-evidence/task-runtime-parallel-workers-20260914/README.md`。

**最新：memory／現行 readiness 修復完成（零模型驗證，不是 full C3）。** 使用者明確選擇現行 Task Pi＋單一 Goal-X 修正，不恢復舊 Goal-Team helper／私有 hold-delivery patches。implementer 移除非必要第三方 writer-memory opt-in，原記憶檔保留、其他角色／模型／工具不變；實際canary discover＋injection及14角色檢查PASS。Goal-X `goal-completion.ts` 僅改用既有合併設定 `loadGoalSettings`，原global disabled被忽略的RED→GREEN，15個current completion／設定／GoalService檔案讀回案例PASS。`check-goal-completion.mjs` 改從實際Pi host解析SDK，不猜巢狀pi-tui路徑；旧私有patch測試及manifest保留為legacy，不重標PASS。現行Task Pi五組suite288/288 PASS；fresh public RPC載入Goal-X0.31.2／subagents0.65.0及契約PASS，0prompt／tokens0／無Task或child。文件 `GOAL-TEAMS.md` 明列現行指令與legacy停用範圍；單檔dependency例外不擴為一般patch權。證據 `goal-team-evidence/task-runtime-readiness-memory-20260914/README.md`。完整live C3及V5仍不宣稱通過；舊r14狀態保持cancelled/paused。

**先前：r14 啟動失敗交接已修復；r12–r14 均未完成 full C3。** 使用者已改為維持原 Terra/Luna 測流程、Task 5,000,000／role 500,000；不是等待便宜模型。r12 實耗80,676超出role80,000；r13 main只把50萬寫在L0 prompt，Worker實際分配12,000；r14補文字後完整prompt6,224超過6,144，timer catch只UI未發failure，L0因此空等。現timer沿用既有failAdmission→failed→collect交接，不加state/controller/retry、不放寬gates。C3 candidate objective精簡並將50萬配額放到Worker可見輸入；同r14 contract唯讀重播新prompt5,685/6,144。實際extension＋mailbox＋collector＋cancel回歸4/4 RED→GREEN，相關286/286 PASS；非新native C3。r14已用原session公開RPC pause/drain收尾：CANCELLED/reservation0/unresolved0、Goal paused、Worker PID消失、target clean。另查出optional-memory強制讀取源自第三方pi-subagents system injection，即使檔案不存在也要求read；先前移除Task提示不等於根治，未patch dependency／全域role。既有readiness fixture/package drift仍未解，未重跑live、未改models。證據 `goal-team-evidence/task-runtime-worker-startup-fix-20260914/README.md`。

**先前控制／輸入修正（模型與執行狀態以下保留為當時歷史，以本頁最新段為準）。** 公開 Task 工具與 observer 共用原有 11 個 parameter schemas 及 Pi 公開 validator；11/11 schemas 完全不變，只讓已證實未執行的 schema 拒絕回到原 agent loop 更正，移除 review selector 特例與重複 guard。合法輸入的執行錯誤、unknown／事件不符、權限／scope／budget／deadline／acceptance gates 仍停止；不猜 ID、不新增 retry／repair API／狀態機。C3 L0 prompt 與 candidate-only `task-runtime/e2e/c3-task-objective.txt` 分離，準備者代入 exact patch path/hash，不重寫歷史 contract；Worker 不轉交 Goal/dispatch/acceptance 指令，配額說明改為 aggregate 而非 leaf quota，移除 optional memory 強制探查，candidate 用原 Git/runtime scope 檢查及 stdin syntax check。真 prepare/mailbox/Worker seam 與 scripted native agent-loop 恢復回歸，最終 **284/284 PASS**；8 Node parser PASS，LSP 有已由当前 bytes 驗證的 past-EOF stale，非全repo綠。r11 的40-file historical manifest unchanged，仍 accepted/paused，不能宣稱 C3 完成；另補記 r11 Worker 配額拒絕與 leaf optional memory、/dev/null 誤判、process substitution 三個錯誤，非只有末尾 ID 錯誤。證據 `goal-team-evidence/task-runtime-causal-control-fix-20260914/README.md`。使用者最新要求後續改便宜模型；目前 models.json 未變、無新模型／Goal／execution／resume。

**agent 執行與 repair／再審政策已修訂（尚非 live 驗收）**：依使用者授權，僅調整 Worker／leaf 提示與新 C3 L0 指引，不新增 repair API、不改 Todo／runtime 狀態機／依賴或驗收 gates。選用 memory 先確認存在；L0 browser check 不由自製 mock 替代；完整填寫 outputSchema，報告只在契約與任務授權內最多局部修正一次。主 agent／Worker 可提出 repair／再審請求，暫停的 Goal 不因此自動 resume，舊 terminal execution 不重開；修復／釐清後仍須 auditor 再審。新準備 spec 必須與政策一致，r6 的舊零修復 contract／失敗不改。r6 最終報告已補 residualRisks，無需再修同一格式；原 auditor 引用 browser／mock，三個 tool errors 是 parent/Astra 後續核對，不混為同一診斷。政策／使用方式：[C3-POLICY.md](task-runtime/e2e/C3-POLICY.md)、[c3-prompt.txt](task-runtime/e2e/c3-prompt.txt)。提示交接回歸 RED→GREEN，final roles/runtime/integration/acceptance **224/224**、3 primary LSP clean；r6-sized Worker prompt 6,119/6,144 bytes，未提高上限。未派 child、未重跑 C3／resume／再審，實際 agent 行為恢復仍未驗。r7 fresh C3 已完成 launch-preflight 但因 cumulative usage 已達 206,553,482/200,000,000（超出 6,553,482）在 Goal／execution 建立前阻擋；無新模型、Worker、Task、browser、review 或 AcceptanceReceipt，證據 `goal-team-evidence/task-runtime-c3-native-retry-20260914-r7/README.md`。

**最新 C3 r11 結果（2026-09-14）**：使用者將 cumulative ceiling 授權提高至 **1,000,000,000 tokens**（authorization `c34a0375`，沿用歷史用量，不重置）；全新 workspace `/home/timmypai/apps/task-pi-todo-c3-20260914-r11`、Goal `mu0n406x-ghosi1`、execution `506392bc-5313-4787-8a40-901fd4f11420`。Worker 精確套用 approved patch（SHA-256 `c763e1aa3f843cc39b207db81f6440d706dbe5af116efb88af5340e161700a10`），managed worktree cleanup complete；指定 Edge browser **12/12**（desktop/mobile，`runtimeErrors:[]`）、fresh independent source-bound review **pass/findings[]**、AcceptanceReceipt/3 `c850df86-5592-432f-ab65-260cb15d2b83` accepted、`targetModified:false`、task complete、execution terminal `ACCEPTED`、reservation closed、unresolved runs 0、target Git-clean。L0 在 acceptance 與 task readback 後呼叫 `team_task_status` 時傳 malformed `execution_id`，public validator fail-closed；沒有 auditor rejection，但 Goal 因 `task-tool-failure` paused，未完成 final Goal completion/audit readback，故 **full C3 不宣稱完成**，不重開 terminal accepted execution、不redispatch、不重播。r11 task usage 352,267、L0 root 490,032；完整證據 `goal-team-evidence/task-runtime-c3-native-retry-20260914-r11/README.md`、`final-state.json`、`evidence-sha256-r11.txt`。修改後 scoped roles/lifecycle/e2e-control **122/122 PASS**；lens full 另顯示舊 evidence TypeScript/Markdown findings及 `role-wave.mjs` inferred-config past-EOF stale，Node parser通過，非 r11 runtime failure。

**原生 retry 政策已修正並完成一次新 C3（2026-09-13）**：使用者確認「原生的retry不應被阻擋」。`run-todo-flow.mjs` 不再把 `auto_retry_start`／`summarization_retry_scheduled` 當致命錯誤；中途 provider error 由後續成功 assistant response 清除，只在原生 `agent_settled` 後仍有最終 assistant/compaction error 時讀最終 usage 並 stop/drain。Pi 保持原設定的有界 retry；預算、deadline、Task/tool failure、未知副作用、review/acceptance gates 不變，仍禁止 Task redispatch。CLI 附加 prompt 區分原生重試與 Task 重做，取代舊 blanket no-retry 指令。observer 全套39/39、2 primary LSP clean。未改 Pi dependency、模型、retry 次數、角色或產品碼。r6 fresh C3（Goal `mtzy19gb-y6xpf4`／execution `c951baf5-4b70-4bcd-87c7-f03d7ae1cb32`）未出現 retry-start，故只證明 native retry 未再被 observer 阻擋；execution 已 AcceptanceReceipt/3 accepted，但 Goal auditor 因 leaf 3 個 procedural tool errors 依 literal strict stop pause，full C3 blocked，不可宣稱 C3 完成。這修復的是測試誤阻擋恢復，不宣稱網路根因已消失。證據 `goal-team-evidence/task-runtime-c3-native-retry-20260913-r6/README.md`；修復回歸證據 `goal-team-evidence/task-runtime-native-retry-fix-20260913/README.md`。以下禁止原生 retry 的記錄為歷史政策，不再適用新 attempt。

**C3 review 證據交接已修復（source／integration 已驗，native C3 尚待重驗）**：依使用者「已換astra模型，請修復」授權，主會話僅修 `integration-review.mjs` 與 `review-runs.mjs`；把既有已驗證 staged host checks 的 criterion 對應、source digest、receipt/log 精確路徑與 SHA-256 封入 `subject.hostChecks`，透過既有 report `requestDigest` 綁定，不新增 report 欄位／通用證據框架／caller selector。沿用 rehearsal 對 input/source/intent/log 的驗證，freeze/readback 再核 receipt bytes 不變；reviewer 讀指定證據並區分 not-assessed 與缺 check，仍保留獨立判斷。回歸先 RED 再 GREEN，相關 integration/acceptance/lifecycle 175/175，最終 focused 8/8、3 primary LSP clean；未派模型 child、未重跑 native/browser、未修改舊 C3/target/依賴/模型/權限或 gates。歷史 request 不回填、不追認；C3 r3 仍 cancelled/paused。證據 `goal-team-evidence/task-runtime-c3-review-handoff-repair-20260913/README.md`。

**C3 r5 native E2E blocked 並已交 Astra（2026-09-13）**：修復後 fresh workspace `/home/timmypai/apps/task-pi-todo-c3-20260913-r5`、Goal `mtzvfhgg-b5n3ul`、execution `a3be211c-09dd-4199-bcb9-a9ba1f8b2c9b` 完成 Worker candidate `ready_for_acceptance`、browser host check verified，且 frozen review request 已含 `subject.hostChecks`。其後 L0 Terra 在 timestamp `1789307670858` 發生 `fetch failed` 並 emitted `auto_retry_start`；runner 依 one-attempt policy 取消 retry、停止 downstream reviewer（`Request was aborted`）、owner drain 後 execution `CANCELLED`、reservation 0、unresolved 0，Goal 已 paused/autoContinue off，無 AcceptanceReceipt。Astra `dcee63b5-de35-4df9-a3c2-6b64e1a99580` 唯讀判定 owning layer 是 provider failure/retry-policy enforcement，不是前次 review evidence binding；早先 WebSocket→SSE fallback 與 terminating `fetch failed` 不可混為同一原因。未修 source、未重試、未改模型/gate/budget/permission；r4 的 prelaunch EEXIST 亦不重用。證據 `goal-team-evidence/task-runtime-c3-review-handoff-repair-20260913-r5/README.md`、`astra-advisory-r5.md`。

**C3 native E2E blocked 並已交 Astra（2026-09-13）**：fresh Goal `mtzsnoky-6wi06a`／workspace `/home/timmypai/apps/task-pi-todo-c3-20260913` 在 `team_task_dispatch` 前置失敗，exact error `goalId required`，execution `[]`；沒有 Worker、browser、review 或 acceptance。根因不是產品程式，也不是應新增 top-level dispatch argument：public dispatch schema 僅允許 `spec_path/benefit/benefit_detail`，而 Task spec 漏了完整 `goalId/taskId/taskRevision` identity tuple。遵照使用者要求未自行修程式或重試；Astra `team.advisor` 已唯讀分析，建議未來在 dispatch 前以新 Goal 回填完整 identity，維持 strict validation。C3 Goal 已 paused、drain settled、workspace clean。證據 `goal-team-evidence/task-runtime-c3-20260913/README.md`；Astra handoff `ASTRA-HANDOFF.md`/`astra-advisory.md`。

**C3 r3 native E2E blocked 並已交 Astra（2026-09-13）**：依前次 Astra identity diagnosis 使用全新 workspace `/home/timmypai/apps/task-pi-todo-c3-20260913-r3`、Goal `mtztg83d-ueta1a`、execution `b64197ff-05a1-4d9c-9528-887cff2c844d`，成功完成 identity binding、Worker `ready_for_acceptance` 與 browser host check（12 scenarios PASS）。但 frozen source-bound review request 未包含已驗證的 host-check receipt，independent review 正確回 `blocked`；execution 已 CANCELLED/reconciled（reservation0、unresolved0、worktree cleanup complete），Goal paused、未 AcceptanceReceipt。Astra `team.advisor` 判定 owning layer 是 runtime review contract/interface（`integration-review.mjs` 建 request 時遺失 host receipt，report schema 也無 evidence binding），不是 L0 sequencing；未自行修改或重試。下一步需另獲授權後以 isolated fixture 修 runtime seam，再做全新 C3；現有 C3 不可接受。證據 `goal-team-evidence/task-runtime-c3-20260913-r3/README.md`、`ASTRA-HANDOFF-R3.md`、`astra-advisory-r3.md`。

**C2 outcome 修復後完整 native E2E 已驗收通過（2026-09-13）**：全新 workspace `/home/timmypai/apps/task-pi-todo-c2-final-20260913`、Goal `mtzs40pu-sa0x05`、execution `a4f89858-7efc-4a8a-8e2d-d0c658c12ba2`。Worker 以 `outcome=ready_for_acceptance` 正確交給 L0；managed patch handoff/terminal/cleanup PASS；Edge browser **12/12**（desktop/mobile、zero runtime errors）、fresh independent source-bound review PASS；AcceptanceReceipt/3 `34b26fe2-2114-49dd-b971-fdd6846858ed` accepted，`targetModified:false`；native Goal/task readback complete，reservation=0，target Git-clean。交付為 verified patch，未套用 target、未部署、未commit/push。runner 的 `fullE2EPassed:false`/exit 1 是 transport sentinel（入口刻意不自行宣稱 acceptance），不覆寫 durable acceptance 證據。證據：`goal-team-evidence/task-runtime-c2-final-20260913-2/README.md`。本次累計快照 **150,296,152/200M**；V3 verify-only 由未完成改為已驗收，V1/V2/V4/V5及approved-integration/D4/D5/D7整體範圍仍依原狀態，不擴大宣稱。

**最新 C2 outcome 語義修復（source-only，完整 C2 仍未驗收）**：`455b4ff3` 真 managed role 套核准 patch/SHA 成功且已 terminal，Worker 卻把僅剩 L0 host/review 的候選封成 blocked；collector當下Worker尚活是下游時序，不是阻塞真因。`636b6cfe` 的外部artifact失敗則是Worker轉述path漏連字號，非已證sandbox限制；撤回先前隔離歸因。已修既有Worker prompt及outcome/status描述：角色成功且terminal、僅L0 gates待驗可ready_for_acceptance；未驗host criteria保持indeterminate，具體阻塞仍blocked；不改validator/state/acceptance或新增fallback。228/228 roles/lifecycle/integration回歸＋原始輸入唯讀replay；無新model/Goal/execution、無product/dependency/permission/gate改動。两次舊execution CANCELLED/reservation0/unresolved0/acceptance0、workspace clean。累計快照148,176,423/200M；上輪artifact-visible摘要漏算歷史，不可作新launch admission。完整證據 `goal-team-evidence/task-runtime-c2-outcome-repair-20260913/README.md`。

**先前 C2 重跑／輸入因果修復（不追認完整驗收）**：使用者於 admission 修復後另准重跑，`c75af93a` 仍因測試準備漏傳 patch 而 blocked。真因是 main 只把 path/SHA/配額補到 L0 prompt，卻未補 Task spec，且要求 L0 只改 Goal/task ID；公開 dispatch 並未丟失已提供的欄位。這次 Worker 已封存 blocked result，163ms 後 L0 collect 成功，abort 訊息發生在封存之後，沒有 timeout；overspend failure branch 未重驗。使用者再准診斷修復後，已建立修正的 Task-visible spec／一致 L0 prompt、乾淨新 workspace，實際 prepare→mailbox→Worker prompt 回歸通過，依可見輸入套 patch 的真 Edge host-only 12/12；無 runtime/產品碼/gate 改動、無新模型/Goal/execution，舊兩個 C2 仍 cancelled/paused。完整 native C2/review/AcceptanceReceipt 待驗。證據 `goal-team-evidence/task-runtime-c2-input-repair-20260913/README.md`；下段「patch輸入未修／未重跑」為先前修復當時狀態。

**Worker admission 失敗交接修復（本機已驗，未重跑 live）**：C2 `41e458f9` 的 pre-abort 原始 usage 純讀重播證實 leaf 實際150,335超過12,000配額，`reviewUsageAdmission` 拒絕正確；原 Worker catch 僅UI/abort而未交回L0，導致collect超時。現在沿既有 `failed` event 保存錯誤／有界stack，Worker進既有QUIESCENT等待owner取消，collect立即失敗；不釋放reservation、不偽造candidate、不放寬usage/owner/acceptance。主代理修改3個runtime檔＋2個測試檔；final scoped120/120，較早full288/289（未修改的model-settings舊工具清單assert仍缺已啟用supervisor，保留失敗）。C2 patch輸入問題未修；舊execution/Goal不重開，真E2E未驗此新source。證據 `goal-team-evidence/task-runtime-admission-handoff-20260913/README.md`；累計token ceiling依本輪使用者核准為200M，非live/retry授權。

**最新驗收：Todo verify-only 真 E2E 已通過（取代下方歷史未完成敘述）**。第十一輪 `2fed5797`／execution `bd787590-6612-49d3-994a-e1309ff2f0cd`：真 Edge 12/12、獨立 native review binding/seal、AcceptanceReceipt/3 `7e2dd8e3-216f-4519-98ce-2b7f9b77ff6b`、Goal `mtyinyvm-camhf5` 與 task complete/readback、ledger goal_commit_state=committed/reservation_open=0、pane closed/L0 reaped 均有原始證據。原 target Git-clean；交付是 verified patch，不是已套用或部署。修正 review selector/running、preflight bridge/路徑 schema，以及原生小數成本 status 的 raw-byte digest；未擴架構或改模型/依賴/安全門檻。formatter 後重新回歸171/171；證據 `goal-team-evidence/task-runtime-final-accept-live-20260912/README.md`。只關本次 V3；batch Goal、approved-integration live、matched A/B、官方升級與未核對的歷史清理不藉此宣稱完成。

**模型設定已依使用者明確授權套用**：`task-runtime/models.json` 持久指定 E2E L0=Terra、Task Pi=Luna，Task內planner/reviewer/security/challenger=Terra，其餘一般工作角色=Luna；既有CLI／公開native model參數接線，不新增代理層、不改全域profiles／主聊天室／thinking／budget。錯誤設定或未配置角色拒絕繼承，L0模型不符在prompt前拒絕，設定納入runtimeDigest。270/270相關回歸；原生Pi Terra及Luna零prompt讀回各total0/exit0（首次Luna探針usage逾時保留，修raw-fd讀取後同協定重驗成功）。未跑完整模型E2E、不解舊reservation。用法`task-runtime/MODELS.md`；證據`goal-team-evidence/task-runtime-model-settings-20260912/README.md`。下列不變更模型的敘述是較早歷史，非本次設定決策。

**最新：82116651 缺口修正（以下舊 checkpoint 保留為歷史）**：此次真 Goal／Task 已啟動，但 fresh clone 缺 repository-local Goal metadata excludes，派實作者前遭 clean-base 拒絕；Worker 封存 blocked 並退出。L0 卻掃錯 runtime 目錄空等約23分鐘，且 collect 原只回 RESULT_READY，未提供候選 outcome。已只在既有入口修正：fresh Todo 建 Goal 前準備精確 Git excludes（D6 scope／其他worktree同名路徑不豁免）；既有 team_task_collect 增有界 wait_ms，讀正式 mailbox＋Worker exit並回實際 candidate；blocked 不可 stage/accept；observer Task錯誤立即drain、SIGINT/SIGTERM先owner drain、SIGKILL保留checkpoint而不偽造cleanup。3 production＋3 tests，**265/265相關回歸、6 primary LSP clean**；無新架構／模型／reload／部署。舊 execution `04f2fa6d-c2a8-40c0-81a9-df5b2daf7f9e` 仍RESULT_READY/blocked、reservation open、Goal未commit；Worker已exit，pane get回not_found但無close收據，不追認清理。先前hosted/result接線已通過245 source回歸，仍非真E2E驗收；本輪未重跑native E2E，V1–V5不關。證據：`goal-team-evidence/task-runtime-e2e-gaps-20260912/README.md`。

**本輪必要接線修正**：`native-handoff`／`review-runs` 已讀公開workflow sidecar（只在pointer省略時；無效明示pointer仍拒絕）及真step `completed`，不改root complete／process-terminal／獨審與budget gates。2 regression RED→GREEN，integration＋acceptance＋lifecycle **155/155 PASS**；現有browser checker格式化後重跑 **12/12 PASS**。真hosted-workflow仍無detached processTerminal，不能以這兩項reader修正宣稱V3完成；原生也拒絕usageBudget＋async workflow child，不能簡單加async或取消budget繞過。終止／執行契約仍待處理，不自行擴架構或追認舊run。原測試L4748為4747行檔案的past-EOF stale診斷；同bytes同目錄fresh primary clean＋Node parser及155回歸確認，無實際label可刪。證據：`goal-team-evidence/task-runtime-native-contract-20260912/README.md`。

**最新 E2E checkpoint（優先跑通，效率優化延後）**：接續 e5fdc2ea 已完成的真 native implementer patch，未重派writer。真Edge先11項PASS，但截圖揭露empty-state仍顯示；既有checker補實際可見性斷言RED，rehearsal app補`.empty-state[hidden]{display:none}`後12/12 PASS（desktop/mobile，無app runtime errors），原target保持Git-clean。完整teams仍blocked：真hosted-workflow status沒有consumer要求的processTerminal/workflowReceiptPath，step狀態completed與consumer complete不符，不能偽造終止／驗收；L0已失效。已透過既有cancel/reconcile保存CANCEL_REQUESTED，核native complete/inventory＋owned PID ticks/無children後退出Worker2437172並關w3:pA；reservation未force release、Goal最後仍active、無獨審或AcceptanceReceipt。可執行app與修後patch、RED/GREEN、source hashes、恢復證據：`goal-team-evidence/task-runtime-todo-e2e-continuation-20260912/README.md`。需修真正native producer/consumer契約，不重做已完成產品、不以browser PASS關V3。下面較早狀態保留為歷史。

## 目標與驗收原則

- Pi／Goal-X／pi-subagents 可持續官方升級；只用公開 API，原則不 patch dependency、不硬編碼版本例外。2026-09-14 使用者明確例外：Goal-X completion 改用既有合併設定函式，以及本頁最新段的 pi-subagents alias／no-start 因果修復。均為限定本機 patch，升級須核來源並重驗，不代表 dependency pristine；不得據此恢復舊私有 helper patches 或取得一般套件修改權。
- Orchestrator 按需求與證據動態選角色、數量、串行／平行；不固定一 implementer 加一 reviewer。
- 單一 checkout 獨佔寫入；多 writer 使用受控 worktrees，整合後重新驗收。
- 完整目標須真 Goal → Task Pi → 動態 leaf → 實際產物／必要整合 → host／review → acceptance → Goal readback → cleanup。
- 統計全部成功／失敗／修復的 parent、Worker、leaf 用量及耗時；cache 分列，不以單一 Worker 降量宣稱整體改善。

## P0 — 本輪開始

- [x] **D0 設計修訂**：更新 TASK-PI-RUNTIME-DESIGN.md v1.2，移除 Task 級單 writer／固定角色配對，補 worktree 決策與 merge/rollback/freshness 邊界。
- [x] **D1 動態 wave admission 與 native 接線（離線）**：新 contract v2 的併發由契約配置；保留 v1 reader。單 wave stable-key `runs.all()`；數量按需求，所有 admission 先檢查再 spawn，不開數個競爭的 top-level roots。
- [x] **D1 隔離／相依 admission（離線；非 OS sandbox）**：共享 checkout 不同時 writer/check/reviewer；多 mutation/check lanes 要 managed worktrees。真 Git root/clean HEAD/base 驗證，非 Git／dirty／stale 拒絕，無 auto stash/commit。隔離不代表外部 ports/DB/cache 已隔離。
- [x] **D1 durable waves（離線）**：保存成員、理由、配額與 native root；所有成員計入 spawn/token budget；部分失敗或 unknown 不自動重派。不得只計 top-level workflow。
- [x] **D2 candidate/host 分離（離線）**：new-contract ready 可包含 host-owned indeterminate；missing non-host evidence、not_met、needs_user 仍拒絕。HostAcceptance 以真正且新鮮 checks 決定，不逼 Worker 在測試前說 met。
- [x] **D2 回歸真失敗（pure validator replay，未動歷史 execution）**：保存 a2d16f35 首次 indeterminate→遭拒→無證據稱 host confirmed 的時序，確保第一份誠實候選即可進 host 檢查且未檢查不能接受。

## P1 — 整合與交付閉環

- [x] **D3a native handoff ingest（source-only／離線）**：公開 status/compiler return/workflow receipt 綁定 key/run/base；canonical bounded handoff/patch 保存 SHA-256 與 durable copy，不猜路徑、不 import 私有 parser。Native 格式 fixture 通過；尚缺真 native allocator／handoff canary。
- [x] **D3a 唯一 integration owner／verify-only（離線）**：controller+epoch/RESULT_READY、原 target clean/base/source 驗證；新 shallow no-hardlinks clone、逐 lane base-index 驗證＋cached three-way apply、scope/regular-file gate與全 staged workspace 快照。既有核准可搬移 checks；不寫 target，重複只 readback。
- [x] **D3a Git 回歸**：binary/add/delete、同檔非重疊合併／重疊衝突、identity/base/symlink/scope 拒絕、dirty/stale target、缺 patch、未完成 intent、失敗 check 不重跑、capture tamper、assume-unchanged 隱藏修改皆有檢查。
- [x] **D3b target authority／apply（source-only／離線）**：approved-integration＋獨立互動確認；封存 target/ref/base/tree/index/workspace/patch，Git verify-HEAD prepared transaction fencing，staged diff apply／反向 rollback，不 commit／移動 ref。雙命令 close proof、獨佔 journal、未知／後續修改拒絕；verify-only 不授權。D4 未接前 required review／非 host-check 的最終 evidence binding 仍 blocked。
- [x] **D3 merge 回歸（真 Git＋合成 native receipts）**：獨立／同檔整合、衝突／缺 patch、stale/dirty/hidden target、before/after-effect failure、mutation signal、missing close、retained lock、dirty rollback、雙次 apply、native temp cleanup／receipt tamper。未知狀態保留，不 force/reset；process crash 的完整 owner-safe recovery 仍屬 D7。
- [ ] **D3 整合後 final gate**：新 v3 approved-integration 已接 target apply proof＋完整review/writer＋target final checks→AcceptanceReceipt/2，明確區分candidate與交付source；非通用繞過worktree gate。verify-only已接AcceptanceReceipt/3，明列verified-patch及target未套用，重用staged checks；第十一輪已真驗verify-only/3，approved-integration/2的live驗證仍未完成。非host-check種類沒有目前需求，不列為本次必做功能，未知種類維持拒絕。
- [x] **D4a review 準備／格式驗證（離線）**：使用者批准新契約由 L0 裁定；opt-in v3 policy、frozen request＋raw patch、public launch-contract／review report 純驗證、legacy／stale／tamper／partial 拒絕。重用 stage 工具的 prepare-review action；無實際 public resolver／native review dispatch，不是 native identity proof 或 acceptance。
- [x] **D4b review transport／run evidence binding（source-only／離線）**：動態 waves、公開 preflight/RPC、plan/intent/started/captured receipts；完整 dispatch envelope、launch digest／role／native identity、session 中成功 structured_output 提交、strict usage、前後 source freshness、跨 wave 不重用與 unknown 不重派。實際 public resolver 零模型探測與 native compiler validation 通過；尚非真 native review canary。當時缺 production admission；本輪 source 接線見 D5/D7，live readiness 仍未通過。TODO-14b71d73。
- [x] **D4c host-owned review candidate（source-only／離線）**：全部已登記且已回收PASS waves才可封存；獨立全inventory身份查核、owner lock、intent/candidate digests、重複完整readback、sealed後拒絕新plan/start。未完成/unknown/BLOCKED/partial/source/capture/inventory tamper均blocked；stage既有tool增加seal-review且拒絕selectors。候選不授權apply/acceptance。TODO-e64811d9。
- [x] **D4d applied proof／after-apply review reader（source-only／離線）**：重用嚴格成功apply journal reader，核雙命令terminal、baseline/target/index/ref、lock/rollback；顯式target apply digest才可讀已封存review，不補寫/重跑或放寬原clean gate。stage既有tool新增read-applied-review；真Git/journal fixture模擬future v3 L0 apply gate，非production放行。TODO-2fc4cd6f。
- [x] **D4e writer／review-bound apply（source-only）**：重驗 durable native identity/terminal/handoff、writer checked/verified evidence/required criteria/runtime checks/reported budget；只讀既存sealed candidate，確認前後相同binding寫入intent/command/receipt，套用後／idempotent重驗，保留native required/status。D4d fixture已改走真HostAcceptance apply gate（native producer/admission/UI仍模擬）。TODO-dfc2358e。
- [ ] **D4 final acceptance 接線**：已套用 target 的 source 路徑接通：final checks、完整review/writer/usage/lifecycle→AcceptanceReceipt/2，重複accept及Goal寫入前後重驗；ledger owner/epoch再核。保留native required/review-required、v1/v2不追認。verify-only patch final另接/3並核原target不變；第十一輪已有真native/Goal/readback/cleanup證據，未涵蓋approved-integration/2，故本項不整體關閉。證據 `goal-team-evidence/task-runtime-final-acceptance.md`。
- [x] **D5a review session 計量（source-only）**：新 Worker/leaf 的 execution-owned session roots、公開 session path、bounded native captures；strict Worker＋leaf＋既有 review 用量含 cache，等待前後重讀、prefix 截短／unknown／overflow／超額／跨 execution 缺歷史拒絕；intent/2 保存計量 checkpoint。真 public SessionManager 序列化與 native-format 負例；無模型／live lifecycle grant。TODO-cd6ae04a。
- [x] **D5b1 v3 Worker role 用量 guard（source-only）**：spawn/spawnWave 重用 strict reader，對照實際 launch/terminal inventory，保存 ref/digest checkpoint、拒絕歷史重置／partial／已計來源縮短；failed leaf/cache仍計，已落盤cancel先處理。v1/v2不變，非live lifecycle grant。TODO-d5fc731f。
- [ ] **D5 budget**：已接 Worker 逐回合／compaction/tree／role admission、初次 SessionManager 未 flush、terminal 後零模型喚醒；依既有 0–1 restart cap 唯讀計前次 Worker/leaf/review（含 failed/cache），在新 execution 保存 prior usage，累計 tokens／spawn／allocation，缺失／篡改／重建不 reset。已接 applied-target與verify-only final acceptance／Goal readback再計量與freshness；仍缺真 native/provider 用量驗收；v1/v2 不追認，無完整舊 corpus 拒絕。checkpoint 不停止已執行的 provider/child，不承諾精確 token ceiling。證據 `goal-team-evidence/task-runtime-budget.md`。
- [x] **D6 原 workspace baseline/scope（新 v3／source-only）**：reserve 前保存完整 sourceRoot／明列 worktreePath baseline；boot、role admission、ready seal 與 host 入口核新增／修改／刪除／mode／空目錄。結果綁全樹快照，collect／checks 前後／首次 stage 拒絕 sourcePaths 外的晚到變更；symlink／敏感路徑／特殊檔案拒絕，FIFO 不阻塞。目前經使用者核准，另只對原專案明列豁免官方`.pi/goals/`，並接到v3 target apply/readback；其他.pi內容、目錄mode、其他worktree同名路徑及Worker overlap限制不變。Git/runtimeRoot豁免仍保留；沿用 bounded reader，v1/v2不變，缺舊baseline不補造。168 runtime／23 host／38 parser PASS；真 native/live scope仍屬V3，非 OS sandbox。證據 `goal-team-evidence/task-runtime-workspace.md`。
- [ ] **D7 failure cleanup／lifecycle**：取消清理 source 路徑已接通：原 controller 有界等候 Worker/native/review drain、PID/start-time exit、pane close reply與atomic reservation CAS；late grant/result、遺失通知、controller重建、Worker崩潰、未知stop/close與owner fencing有回歸。原始native bytes保留供中斷後重驗，不冒寫Worker事件、不重派、不操作歷史execution。本輪另接 L0 instance/PID/session/epoch admission、session close 撤銷、禁止有 open reservation 時 switch/fork；重建只 reconcile，不繼承 dispatch。另接 accepted後matching Goal readback→idle pane close journal→release；close unknown保留且不重播。不是 TTL lease；真 native/Herdr/provider/session lifecycle canary 仍缺，因此 D7 不整項關閉。證據 `goal-team-evidence/task-runtime-cancellation.md`。

## 範圍確認

- **取消「增加其他驗收證據種類」待辦**：使用者確認非必要功能不做。既有 Todo E2E criteria 均使用 host-check，沒有具體非host-check需求；不得將未支援能力自動列為必做項目。不增加證據框架或契約欄位，現有未知種類／source不匹配拒絕條件保留。這不是放寬已指定的required gate；未來若有明確新需求再另行確認。

## P2 — 真實驗證與效能

- [ ] **通用 request-to-outcome 驗收（現行主線）**：由真使用者需求＋source／授權限制開始，不預製Task specs／答案patch；核L0自行拆分的需求涵蓋、依賴、scope/checks、總計量，及真執行／必要整合／原始要求逐項結果。G1/G2/G3或既有C3 receipts只能支持其protocol／固定輸入範圍，不代替此項。需另獲live授權及現行入口載入／完整accounting；不自行建立Goal或重派舊execution。

2026-09-12 公開入口稽核：已接既有`TEAMS_E2E_CANARY`至public initialize及Herdr running gate，canary不偽造readiness；30/30 scoped回歸、3primary clean。隨後使用者核准一次30分鐘／含cache共200萬tokens的隔離真E2E。公開RPC載入成功，但真Goal `mtx6b80k-31cjtg` 的set_goal_tasks因RPC custom UI回傳undefined、讀decision失敗，已暫停且0 tasks；無Task Pi dispatch、ledger本project0 executions、未進browser/acceptance。外層observer誤等報告檔至30分鐘才退出，PID1492454已reaped。另真Goal新增`.pi/.goals-pool-snapshot.json`在已核准豁免外，未自行擴scope。**預算失敗**：main漏計自身準備／稽核，已知main快照5,033,829＋isolated L0 59,168＝至少5,092,997 tokens，超出核准200萬；不重跑／不再派工。證據：`goal-team-evidence/task-runtime-entrypoint-20260912/README.md`及`goal-team-evidence/task-runtime-todo-public-20260912-002159-43d4a419/README.md`。V1–V5仍未驗收；再次執行需owner新決定，不沿用本次失敗授權重跑。

- [ ] **V1 零模型完整 gates**：D1/D2 已有 29 runtime tests、43 handoff cases、native script validation、真 Git clean-base／dirty／stale／nested／non-Git fixtures、缺 policy 零派工拒絕。D3a 有 40 runtime／43 handoff／23 host-evidence PASS；D3b 新增 staged apply/rollback 離線回歸（見證據）。仍缺真 native allocation/handoff 與 D4–D7，不能把 fixture 當 full E2E。
- [ ] **V2 真 disposable Goal**：single/batch completion/readback；不使用 projection simulation，不碰既有 Goal。第十一輪 single blocking task→authoritative receipt evidence→Goal readback/complete 已真驗；batch 尚未驗，不整項關閉。不擴Worker write scope、不操作舊 Goal。
- [x] **V3 Todo verify-only 全流程**：第十一輪以既有App patch走真公開RPC、動態native managed writer、staged host Edge12/12、fresh no-write review、AcceptanceReceipt/3、真Goal task/Goal readback及cleanup。source/patch/check/usage/native digest均讀回，原target未改；不含正式apply/deploy或其他模式。證據 `goal-team-evidence/task-runtime-final-accept-live-20260912/README.md`。
- [ ] **V4 matched A/B**：同規格／模型／budget／驗收做 direct vs Task Pi；完整交付時間、input/output/cache、返工、人工介入與恢复能力；小任務可維持 direct 更優的結論。
- [ ] **V5 官方升級 gate**：backup→pristine→peer/engine→offline→fresh load→public capabilities→live native handoff/terminal→真 Goal readback。未取得完整 readiness 維持 direct-only。

## 已有基礎（非 full acceptance）

- [x] canonical contracts、SQLite ledger v2／CAS／reservation、durable mailbox v0/v1。
- [x] Worker boot/grant/bound、Herdr adapter、source capture、HostAcceptance／Goal public event guard prototype。
- [x] 公開 RPC spawn/stop/status、observed process proof、takeover proof＋明確確認 CAS。
- [x] 23 runtime tests／43 handoff cases 的上一版 offline 基線；本輪修改後須重新驗證。
- [x] dependency pristine、撤回 private import／chord/版本通過特判；generic readiness＋source freshness。
- [x] usage 成功／失敗皆計，native steps.sessionFile 定位 leaf，unknown 不是零。

## Live 證據與未結案項目

- **2026-09-12 最新新專案重跑：BLOCKED，非成功交付。** 使用者要求新webapp與自行修復後，新Goal `mtxl4c2w-l1n4na`／blocking task／Task Pi execution `62b0c1a4-f27c-4ac2-ab04-ac85a9ced219` 真正建立；pi-subagents 0.66.0背景runner要求Pi 0.84.4缺少的`chord`，首wave unknown，沒有App／browser／review／AcceptanceReceipt。Goal已暫停，L0已reaped；Worker PID2190473/startTicks3473629仍活著，CANCEL_REQUESTED與native terminal缺證據未解決，不稱cleanup成功。最新已知用量快照7,683,950含main/L0/Worker/cache，leaf未知及其後audit另計，停止新增模型執行。已修原source精確pool snapshot regular-file豁免（非全部.pi／Worker寫入權）及host-check busy時短stats timeout；44/44 final scoped tests、7 primary/scoped cache clean，非fullE2E。官方0.65.0僅下載／核原始alias，未安裝、未降版、未證完整相容；第三方inspection副本被工具自動修改的差異保留，原tarball完整。L0 wait忽略early unknown至timeout仍未修／live驗證。完整checkpoint與安全恢復順序：`goal-team-evidence/task-runtime-todo-retry-20260912/README.md`。以下較早的source-only與scope未批准敘述為歷史狀態，不代表最新。

- 2026-09-12 確認入口／測試控制source修正：既有`run-todo-flow.mjs`改為prepared Todo的公開RPC入口，移除舊Node controller／Goal projection；明確確認開關才准spawn，Goal pause／tool failure＋agent_settled後取fresh final stats即退出，不等報告檔。main授權entry起的strict用量＋L0 stats＋Task完整預留額做admission，未知／超額／prefix異動拒絕，無retry；僅回收owned L0、不假稱Worker cleanup或fullE2E。43/43 scoped＋2parser／2primary／scoped lens通過，證據`goal-team-evidence/task-runtime-e2e-control-20260912/README.md`。未跑native/model E2E；pool snapshot scope仍未放寬，main在途／observer外費用仍需owner同anchor控管，不宣稱全域硬額度。V1–V5保持未完。

- 239aa32c：252,911ms，真 app／Edge 11 PASS，但 review-required、budget20,000/46,194、越界 artifacts、Goal simulation；舊 harness 錯誤 acceptance 保留，由 main audit 否決。
- a2d16f35：521,205ms，真 app／Edge 11 PASS，HostAcceptance 正確拒絕 review-required；leaf budget12,000/62,946。Worker 12 messages／11 reads，等待 leaf 期間無 read 輪詢；但 Worker 虛報 host 已驗收。未建真 Goal。
- a2d16f35 Worker＋leaf 1,196,378 tokens（含1,016,064 cache）；全程更慢、非 cache 較前輪增加，不是 matched A/B。
- 5 舊 execution＋a2d16f35 ledger 未完整 reconcile。新觀察 Herdr 已無 w3:p8；不以 pane 不存在代替 ledger cleanup。
- 主 Pi／Goal-X 不升級；隔離 Worker Pi 僅既有 canary prefix。Herdr update 仍須 detach 後另安排，不在本輪。

證據：`goal-team-evidence/task-runtime-todo-e2e/README.md`、`goal-team-evidence/task-runtime-todo-fresh-2026-09-10T09-09-33-688Z/README.md`。本輪 D1/D2：`goal-team-evidence/task-runtime-dynamic-20260910/README.md`，TODO-3c73dce0；母項 TODO-a955d56c 仍 open/in_progress。原始 PASS／failure／audit 不覆寫，新修訂另存證據。

D3a 證據：`goal-team-evidence/task-runtime-integration-20260910/README.md`，TODO-7c30e913。D3b 證據：`goal-team-evidence/task-runtime-apply-20260910/README.md`，TODO-5dc25b67；只在 disposable Git fixtures apply/rollback，沒有實際 merge 使用者 repo。D4a 證據：`goal-team-evidence/task-runtime-review-20260910/README.md`，TODO-8bb67ecb。D4b source-only 證據：`goal-team-evidence/task-runtime-review-runs-20260911/README.md`，TODO-14b71d73；71 runtime／23 host checks PASS，native script validation PASS，public resolver 可載入，但未提供 live registry 的 probe 仍為 host_required，不能當 launch approval。43 handoff 為上次紀錄，本輪未重跑未修改的 legacy checker（含 private adapter）。D4c source-only 證據：`goal-team-evidence/task-runtime-review-candidate-20260911/README.md`，TODO-e64811d9；包含完整inventory封存、身份重用負例與實際stage tool callback的隔離host測試，非native/live E2E。D4d 證據：`goal-team-evidence/task-runtime-applied-review-20260911/README.md`，TODO-2fc4cd6f。D4e 證據：`goal-team-evidence/task-runtime-review-authority-20260911/README.md`，TODO-dfc2358e；writer/review-bound apply 已走真 L0 gate，非 native live producer／final acceptance。D5a 證據：`goal-team-evidence/task-runtime-review-usage-20260911/README.md`，TODO-cd6ae04a；review actual-usage guard 已接，production lifecycle assertion 仍缺，final acceptance 仍 blocked。D5b1 證據：`goal-team-evidence/task-runtime-role-usage-20260911/README.md`，TODO-d5fc731f；v3 role-spawn 局部 guard 已接，非整個 D5 完成。最新 source-only 證據 `goal-team-evidence/task-runtime-budget.md/.log`：204 runtime／23 host／39 parser，10 primary clean；逐回合／一次重試 cumulative usage 與 L0 instance admission 已接。最新 final acceptance 證據 `goal-team-evidence/task-runtime-final-acceptance.md/.log`：已套用target→final checks/usage→AcceptanceReceipt/2→Goal guard/readback→cleanup 的零模型consumer路徑已通。verify-only另接明確patch交付/3（非target已套用），最新完整223/223、23host/39parser與39SHA前後一致；4primary clean，scoped cache acceptance仍1warning/2EOF stale，非全repo綠。歷史209/210＋單例修正及本輪RED保留。尚缺真native/Goal/Herdr/E2E、A/B及官方升級；不得自動派新模型 canary、reload、放寬 required gate 或操作既有 Goal/reservations。

最新Goal metadata source修正證據同檔：full226/227＋舊exclusion assert更新後1/1（非第二次full run），23host/39parser，僅該test檔在full後改動且其餘38/39 source相同。6primary clean；scoped lens workspace-scope仍1warning/1EOF stale，不改source迎合。Native Goal/model/Herdr仍未live驗收，沒有新微TODO／工具／架構或歷史資料操作。
