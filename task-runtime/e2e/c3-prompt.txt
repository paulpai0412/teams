Run one fresh verify-only C3 Todo E2E in the prepared workspace. You are L0: you own Goal control, Task dispatch, staging, designated host checks, independent source-bound review, acceptance and Goal readback. Worker owns candidate-role coordination, not these L0 operations. Preserve configured models, budgets, deadline, scopes and gates. No product/runtime repair, deployment, commit or push.

Preparation: read SPEC.md and the prepared .git/task-pi-spec.json. Its objective must be the candidate-only c3-task-objective.txt with the exact approved patch path/SHA-256 substituted by the parent; its nonGoals must constrain candidate work, not instruct Worker or leaf to create Goals, dispatch Tasks or run final acceptance. Do not pass this L0 prompt as the Task objective or leaf assignment. If preparation conflicts with the approved policy, report the conflict before creating a Goal or dispatching; never rewrite a sealed contract.

Create exactly one disposable regular Goal, without a Goal token_budget, and one block_completion=true todo-e2e task. The Goal objective must retain the completion and error policy below; it is not the Task objective. Bind only .git/task-pi-spec.json goalId from pending to the returned Goal ID, preserving taskId=todo-e2e, taskRevision=1 and all other fields. This control-metadata bind is allowed; app/ writes from L0 are not. Verify the tuple and dispatch with the absolute spec_path, benefit and benefit_detail only.

Collect the same execution with wait_ms=1200000; no shell polling or Task redispatch. A successful candidate is ready_for_acceptance with terminal roles and native managed-worktree handoff. Pending L0 host criteria stay indeterminate. A blocked/failed candidate cannot be staged or accepted: reconcile/cancel only its execution and pause only its Goal.

For a ready candidate, stage, run the designated browser check, prepare/plan/start/collect the independent source-bound review, seal review, and accept through the existing public tools. Reuse unchanged verified evidence, not repeated checks. Complete the matching Goal task with the authoritative AcceptanceReceipt reference, read back Task/Goal state, then request Goal completion through its public tool. Task complete or execution ACCEPTED alone is not Goal completion; final audit and readback remain required. Completion needs fresh browser evidence, independent review, AcceptanceReceipt/3 targetModified:false, matching Goal/task readback and terminal usage/reservation/cleanup evidence.

Error policy: preserve each original error and establish its effects. A native schema-rejected Task input has not executed; correct its arguments using the returned identity through the existing agent loop, within budget/deadline. Never guess or truncate an ID, redispatch an existing Task or replay uncertain effects. Schema-valid execution errors, unknown launch/terminal state, source/evidence mismatch, failed authority/budget/deadline gates or unresolved required evidence require owner-safe stop/reconciliation. Pi's configured bounded assistant/summarization retries remain allowed; they are not Task retries.

Product failure requires separately authorized causal repair. Verification-tool failure is inconclusive, not permission to change the product; the designated host evidence may resolve the product gap without claiming a failed substitute check passed. Interpret browser diagnostics by actual checker semantics: navigation abort/DevTools announcement alone is nonfatal; runtime errors, failed assertions and missing assets block. At most one authorized report-only correction in the same run may use existing evidence; maxReportRepairs is not an allowance for tool-effect replay. No implementation/check rerun or writer relaunch for formatting.

If the auditor rejects, preserve every objection, evidence, owning layer and proposed correction/counter-evidence. Request bounded repair/re-audit through supported owner controls; requesting is not approval. Do not resume a paused Goal without explicit owner authorization, reopen an accepted/cancelled execution, force completion or invent an unavailable API. A permitted later phase is recorded separately, not an error-free relabeling of the original attempt. Re-audit needs addressed objections and current owner/state/budget prerequisites; no unchanged repeated completion requests. Escalate to Astra only for a material unresolved question after ordinary evidence analysis.
