# Parent decision after diagnosis

Mission 3ee017f2-4571-46c2-85b1-1b907bc003e8; diagnosis workflow e8f4af03-b2d6-4693-89d8-c28a8cb03411, child 43919142-95f7-4bda-9ffe-71f4dddf4c48, native terminal complete.

Accepted cause: GoalRuntime has no exact child binding/hold input; its real queue emitted a checkpoint in 25 ms in diagnose/hold-wake-probe.json. Existing result watcher calls notifier before async-complete event. Main inspected raw JSON and actual notify.ts sendCompletion/deliver.

Correction to diagnostic recommendation: native registerSubagentNotify already calls sendMessage with triggerTurn (default true), so absence of Goal async-complete listener is NOT proof main cannot wake. Do not add a second checkpoint wake in response to ordinary native completion. Do not automatically clear teamGoalActiveStep: main alone reconciles that marker after reading process proof/receipts and actual source. Bridge only holds/relinquishes its own continuation suppression and surfaces reconciliation state. User-paused goals may receive native evidence notifications but must not have Goal status/autoContinue changed or fresh task dispatch.

Approved minimal implementation slice: sole writer may edit installed pi-goal-x/extensions/goal-runtime.ts, goal-state.ts, goal-events.ts, goal.ts and add a small goal-team-hold.ts module if needed; use only the subset necessary. No _goalCore coupling. Use existing public Pi lifecycle/tool result/session-entry and pi-subagents RPC status/native events where practical; never parse human transcript prose or import subagent private implementation as production API. Exact Goal/task/session/run/cwd correlation and status freshness required. Goal-side adapter registers through existing installed extension (no new ambient extension/config change). goal-task-step.js may gain a bounded binding projection if genuinely required; preserve existing no-replay/intent/active-step ownership invariants. pi-subagents production source remains read-only unless implementer demonstrates a missing public seam to parent before editing.

Allow related runnable regression scripts and docs in /home/timmypai/.pi/agent/teams: check-goal-hold-wake.mjs (and tightly scoped support under evidence directory), check-goal-team.mjs if helper expectations change, GOAL-TEAMS.md for new operation/limitations, and evidence/hold-wake-20260906 implementation artifacts. Never edit other global policies/profiles/settings, existing goals/sessions/missions or earlier evidence. Preserve diagnostic raw artifacts.

Before first mutation backup each existing changed file with SHA256 and path mapping under evidence/hold-wake-20260906/backup; record new files. Do not reload/restart this live parent or unrelated sessions. New installed package source takes effect only in a new/reloaded session; npm upgrades overwrite patch, document selective rollback/reapply. No dependencies/tools/provider installs. No model workers through CLI/SDK; tests may drive real installed callbacks with disposable files/no model calls. Retain one runnable check with actual integration callbacks and explicit mocked boundaries. C1-C5 remain unchanged. Unknown/missing status must return control for reconciliation, not invent running proof or permanently hold on a stale marker. Avoid background polling; bounded on-demand RPC reads/timeouts at existing lifecycle events are acceptable, no new scheduler.

Required final test matrix: real queue + installer/event registration + helper launch-result binding + native notifier completion ordering in one exercised path; active vs unrelated/no-binding; success/failure dedupe; user pause/stop/unfocus and pause-then-resume/newer task-run; changed/foreign sessions and cwd; start/event/result races; reload/retained running/terminal/unknown/missing status; queued checkpoint before bind; process proof distinctions. Record measured continuation/wake counts rather than promising global zero tokens. No-write security review and fresh correctness review remain required after independent verifier.
