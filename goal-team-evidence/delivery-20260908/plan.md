# Delivery repair — approved by user 開始修正 / 繼續

Main-only; no child, existing Goal/mission mutation, model/permission/concurrency change, new dependency, reload, publish or deploy.

Implementation slices and public checks:

1. Reusable parent host-check receipt: format first using installed formatter; capture declared source scope before/after a trusted argv command; persist output/exit and source digests; reject stale source/logs and duplicate execution. Test real CLI with disposable files/subprocesses, not a model verdict.
2. Reuse the same receipt validation in a minimal declared acceptance contract. Bind optional new Goal verification_contract references to immutable contract digests; check at task/Goal completion mutation boundaries. Legacy Goals are not silently opted in. Evidence contents/subjective acceptance remain main-owned, not automatically proven by hashes.
3. Extend existing goal-request preparation to output native args/state setup, compute declared source fingerprint, carry task timeout without changing the Goal child result schema. No additional controller/task store.
4. Classify role-readiness failures without weakening required skills, summarize selected missing dependencies; inspect requirements before proposing replacements. Document minimal check reuse and real-task metrics, no synthetic speed claims.

Required gates: scoped regression, formatter idempotence, final source/receipt freshness, patch compatibility/recovery checks and main inspection. Independent lifecycle/security review and live Goal/TUI canary remain REQUIRED but unavailable because user declined children; no full acceptance claim. Browser E2E is N/A for the non-visual host-check CLI; live Goal/TUI is not N/A.

Sources: prior reliability v2 remains COMPATIBLE/post; prior final-source ledger has formatting drift and must stay historical. Back up exact current files before edits; do not overwrite prior hashes. Candidate must pass before installation; no repeated apply/revert to discover outcomes.
