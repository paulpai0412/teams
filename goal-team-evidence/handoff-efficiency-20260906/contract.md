# Handoff / efficiency repair — parent-only

User authority: implement the final proposal; no child/model launch, no Goal focus or mutations.

## Acceptance

1. OutputSchema is the sole child format contract; generic profile instructions cannot mandate extra keys. All three writers require the native sibling acceptanceReport without changing evidence requirements.
2. Ordinary structured handoffs use one versioned host-prepared schema; native validation and effective role/capability preflight happen without a model. The helper does not launch, resume, run commands or accept the product.
3. Invalid/missing reports preserve artifacts and never cause automatic writer replay. Existing goal helper stays byte-identical; gate replay/crash recovery retains evidence instead of re-executing validations.
4. Report validity is not product acceptance. Raw evidence, final source and real entrypoint remain parent/host responsibilities.
5. Main defaults Sol high; planner Sol high, implementer Luna high (task-scoped upgrades), advisor Astra medium. Disable approved loop/control/workflow/brainstorm/gateway/remote-pi entrypoints; preserve installed sources and unrelated resources.
6. Timeout explicitly task-selected by parent; keep existing safety ceilings, no automatic retry or mandatory role sequence.

## Validation

parent-only: native config discovery in home/Grafana, native schema/acceptance fixtures, goal replay regression, gate replay regression, LSP and scoped lens, protected file hashes. isolated-only: disposable writer artifact test. No new dependencies.
Independent child review: N/A, user main-only. Live child/model/TUI E2E: not run, no authority to start canaries. No product or speed benchmark claim.

Backup: /home/timmypai/.pi/backups/team-handoff-20260906-115903/manifest.json
Protected: goal-task-step.js and installed goal hold/wake sources; no edits to package implementations, credentials, project source, Goal records or budgets.
